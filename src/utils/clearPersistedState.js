// import { PURGE } from 'redux-persist';


// export const clearPersistedState = (builder) => {
//     builder.addCase(PURGE, (state) => {
//         customEntityAdapter.removeAll(state);
//     });
// };


// I need to replace customEntityAdapter with the adapter or logic for the specific slice you want to clear.